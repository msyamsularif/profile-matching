<?php

include("../koneksi.php");

// cek apakah tombol daftar sudah diklik atau blum?
if(isset($_POST['input'])){

    // ambil data dari formulir
    $nama_kategori = $_POST["nama_kategori"];
    $id_kriteria_kategori = $_POST["id_kriteria_kategori"];

    // buat query
    $query = "INSERT INTO kategori_penilaian VALUE ('','$nama_kategori', '$id_kriteria_kategori')";
    $row = mysqli_query($conn, $query);

    // apakah query simpan berhasil?
    if( $query ) {
        // kalau berhasil alihkan ke halaman index.php dengan jam=sukses
        header('Location: index.php');
    } else {
        // kalau gagal alihkan ke halaman indek.php dengan jam=gagal
        header('Location: index.php');
    }


} else {
    die("Akses dilarang...");
}

?>
